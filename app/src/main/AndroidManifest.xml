<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shoppingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon_img"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon_img_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity" />


        <provider
            android:name="com.here.sdk.engine.InitProvider"
            android:authorities="com.here.sdk.engine.InitProvider"
            android:exported="false"
            tools:node="remove" />

        <activity android:name=".MainActivity2" />
        <activity android:name=".MainShopScreenActivity" />
    </application>

</manifest>